import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  menuButton: {
    size: "18px",
    marginLeft: "38px",
  },
  payButton: {
    marginLeft: "10px",
  },
  moreButton: {
    marginLeft: "10px",
    paddingTop: "10px",
  },
}));

export default useStyles;
