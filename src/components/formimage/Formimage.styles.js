import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  imgStyle: {
    width: "70px",
  },
  alignStyle: {
    textAlign: "center",
  },
}));

export default useStyles;
