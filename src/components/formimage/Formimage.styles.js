import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  imgStyle: {
    width: "70px",
  },
  alignStyle: {
    textAlign: "center",
  },
  logoimgStyle: {
    width: "30px",
  },
}));

export default useStyles;
