import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  bodypaddingStyle: {
    paddingRight: "10px",
    paddingLeft: "10px",
  },
  bodytextpaddingStyle: {
    paddingLeft: "10px",
  },
}));

export default useStyles;
